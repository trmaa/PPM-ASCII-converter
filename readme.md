# ASCIICONV

Is a program to convert ppm images to ascii. It comes along a shell script to
convert images to ppm.

## Usage

```console
$ ./convert_img_to_ppm in.jpeg in.ppm
$ ./asciiconv in.ppm
$ cat out.txt
```
